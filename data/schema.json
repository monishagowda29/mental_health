{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MindScan Training Dataset Schema",
  "description": "Expected CSV format for training the BERT mental health classifier",
  "type": "object",
  "required": ["text", "label"],
  "properties": {
    "text": {
      "type": "string",
      "description": "Raw text of the Reddit post or comment (English)",
      "minLength": 5
    },
    "label": {
      "type": "string",
      "description": "Mental health classification label",
      "enum": ["normal", "anxiety", "depression"]
    }
  },
  "example_row": {
    "text": "I haven't been able to sleep for weeks. Everything feels hopeless.",
    "label": "depression"
  }
}
